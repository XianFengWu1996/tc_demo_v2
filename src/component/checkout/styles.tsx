import { Grid, styled } from '@mui/material';

export const CheckoutGrid = styled(Grid)(() => ({
  minHeight: '100vh',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'flex-start',
  alignItems: 'center',
}));
